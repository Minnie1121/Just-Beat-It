# create grids for drawing circles
# DELETE

def createGrid(canvas, data):
	for row in range(5):
		for col in range(5):
			data.grid[row][col] = 

			"""
			canvas.create_rectangle(20+col*cellW, 40+row*cellH,
									20+(col+1)*cellW, 40+(row+1)*cellH,
									width = 0)
			"""